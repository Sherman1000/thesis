# Variables para el contenedor de PostgreSQL en producción.
# Copiar a .env y ajustar si es necesario:
#   cp .env-example .env
#
# El backend Django lee estas mismas variables para conectarse a la base.
# No usar en desarrollo local (ahí se usa docker-compose-local.yml con SQLite).

POSTGRES_USER=hello_django
POSTGRES_PASSWORD=hello_django
POSTGRES_DB=hello_django_dev
